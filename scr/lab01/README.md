## ЛР1 — Ввод/вывод и форматирование

### Задание 1

В первом задании я сначала реализовал счет данных с помощью команды **input()**, после чего присвоил эти данные переменным **name** и **age**. Далее с помощью **f-строки** и комманды **print** я вывел: Привет **name**! Через год тебе будет **age**+1

![first_task](/images/lab01/01_greeting.png)

### Задание 2

Во втором задании я считал входные данные и применил к ним **replace**, чтобы проходили десятичные числа как с точкой, так и с запятой. Потом преобразовал их в вещественный тип данных и присвоил переменным **a** и **b**. После чего вывел их сумму и среднее арифметическое, которое я округлил до двух знаков после запятой с помощью форматирования строк

![second_task](/images/lab01/02_sum_avg.png)

### Задание 3

В этом номере я распаковал в три переменные генератор, который считал входные данные. Потом с помощью формул, данных в задании, программа посчитала **base**, **vat_amount** и **total**. С помощью комманды **print**, f-строки, символа перевода на следующую строку и форматирования получил вывод по строкам с двумя знаками после запятой.

![third_task](/images/lab01/03_discount_vat.png)

### Задание 4

Тут я сразу преобразовал входные данные в **int**, далее создал две переменные, поделив минуты нацело на 60 я получил количество часов и записал их в переменную **hours**, а в переменную mins я записал остаток от деления минут на 60. В конце вывел эти переменные в формате, указанном в задании.

![forth_task](/images/lab01/04_minutes_to_hhmm.png)

### Задание 5

В пятом номере я считал входные данные, а потом убрал ненужные пробелы функцией **split**, которая сделала список **name** из трех элементов. В команде **print** я взял первую букву каждого элемнта списка **name** и превратил их в строку без пробелов с помощью **join**. Длину я нашел путем сложения суммы длин всех элементов списка **name** и разности количества эл-ов спика и единицы.

![fifth_task](/images/lab01/05_initials_and_len.png)

### Задание 6

Здесь я сначала считал входные данные, преобразовав их в **int**, и присвоил их переменной **n**. Затем с помощью цикла считал id всех участников, записывая их в виде списка в переменную **name**. Потом с помощью условных операторов определил сколько людей прийдет с очного и с заочного форматов обучения. И в конце вывел все коммандой **print**.

![sixth_task](/images/lab01/06.png)

### Задание 7

В этом номере я нашел первый символ оригинального текста, перебрав строку и выбрав только заглавные буквы. Потом я взял первый элемент полученного списка и нашел его индекс в исходной строке. Далее я нашел индекс первой точки. В комманде **print** я прошел срезом по строке с шагом 3 символа, начиная от **first_symbol_id** и заканчивая **dot_id+1**.

![seventh_task](/images/lab01/07.png)